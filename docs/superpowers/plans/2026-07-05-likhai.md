# Likhai (لکھائی) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build Likhai — a Roman Urdu → Urdu script converter with a Pakistani truck-art identity, shareable "Truck pe laga do" cards, a consented open-corpus pipeline, and a public two-engine benchmark.

**Architecture:** SvelteKit (Svelte 5 runes) app on Vercel. Two conversion engines (Claude `claude-sonnet-5` and a dictionary baseline) behind one `ConversionEngine` interface (strategy pattern). Server routes keep the Anthropic key and Turso credentials private. Card studio renders real Nastaliq fonts over pre-generated static art and snapshots to PNG client-side.

**Tech Stack:** SvelteKit + TypeScript, Vitest, `@anthropic-ai/sdk`, `@libsql/client` (Turso), `html-to-image`, `@fontsource/noto-nastaliq-urdu`, `@fontsource/bricolage-grotesque`, Playwright (python harness) for e2e.

## Global Constraints

- Spec: `docs/superpowers/specs/2026-07-05-likhai-design.md`. When in doubt, the spec wins.
- Model ID is exactly `claude-sonnet-5`. Temperature/defaults otherwise; `max_tokens: 1000`.
- `/api/convert`: input cap **500 chars**, rate limit **10 requests/min per IP**, secrets fail closed (missing env → 500, never fake output, never silent dictionary fallback).
- Corpus writes happen ONLY when the consent flag is checked; consent default is OFF; no personal data stored.
- Urdu text always rendered by real fonts (Noto Nastaliq Urdu). Never bake Urdu text into AI-generated images.
- Teaching comments per Moiz's CLAUDE.md: plain-English file header block, WHAT/WHY before major functions, `// CONCEPT:` one-liners on first use of novice concepts, no obvious-line comments. Code blocks in this plan include the key ones; keep them when copying.
- Design: Tier A truck art (see spec "Visual identity"). No Inter/Roboto/system-font defaults, no purple gradients.
- After each task: run `/code-review` (Moiz's standard process) before moving on.
- Commit messages end with: `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>` (omitted from snippets below for brevity — always append it).

---

### Task 1: Scaffold SvelteKit project + tooling

**Files:**
- Create: entire SvelteKit scaffold at repo root (`/Users/moizrana/30 in 30 apps/likhai`)
- Modify: `package.json` (scripts, deps)
- Create: `.env.example`

**Interfaces:**
- Consumes: nothing (first task).
- Produces: a running dev server (`npm run dev`), `npm test` wired to Vitest, deps installed for all later tasks.

- [ ] **Step 1: Scaffold into the existing repo**

The repo already exists with `docs/` committed. Scaffold in place:

```bash
cd "/Users/moizrana/30 in 30 apps/likhai"
npx sv create . --template minimal --types ts --no-add-ons --install npm
```

Expected: SvelteKit files created (`src/`, `svelte.config.js`, `vite.config.ts`, `package.json`). If the CLI refuses a non-empty directory, scaffold into a temp dir and move files in (everything except `.git/` and `docs/`).

- [ ] **Step 2: Install dependencies**

```bash
npm i @anthropic-ai/sdk @libsql/client html-to-image
npm i -D vitest tsx dotenv @fontsource/noto-nastaliq-urdu @fontsource/bricolage-grotesque
```

- [ ] **Step 3: Wire Vitest + scripts**

In `package.json` add to `"scripts"`:

```json
"test": "vitest run",
"test:watch": "vitest",
"eval": "tsx scripts/run-eval.ts",
"migrate": "tsx scripts/migrate.ts"
```

Create `.env.example`:

```bash
# CONCEPT: env vars — secrets live outside code so they never end up on GitHub.
ANTHROPIC_API_KEY=sk-ant-...
TURSO_DATABASE_URL=libsql://likhai-corpus-<org>.turso.io
TURSO_AUTH_TOKEN=...
```

Confirm `.gitignore` covers `.env` and `.env.*` (sv scaffold does; verify).

- [ ] **Step 4: Verify dev server and tests run**

```bash
npm run dev -- --port 5173 &   # expect: VITE ready, page at localhost:5173
npx vitest run                  # expect: "No test files found" exit 0 (or passWithNoTests)
```

If Vitest errors on zero tests, add `test: { passWithNoTests: true }` to `vite.config.ts`.

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "chore: scaffold SvelteKit + tooling for Likhai"
```

---

### Task 2: Engine interface + dictionary engine (TDD)

**Files:**
- Create: `src/lib/engines/types.ts`
- Create: `src/lib/engines/dictionary-data.ts`
- Create: `src/lib/engines/dictionary.ts`
- Test: `src/lib/engines/dictionary.test.ts`

**Interfaces:**
- Consumes: nothing.
- Produces (used by Tasks 3, 4, 11):

```ts
export type ConversionResult =
  | { ok: true; urdu: string }
  | { ok: false; reason: 'not_roman_urdu' | 'engine_error' };

export interface ConversionEngine {
  name: string;
  convert(roman: string): Promise<ConversionResult>;
}

export const dictionaryEngine: ConversionEngine; // from dictionary.ts
export const WORD_MAP: Record<string, string>;   // from dictionary-data.ts
```

- [ ] **Step 1: Write the failing test**

`src/lib/engines/dictionary.test.ts`:

```ts
// Tests for the dictionary baseline engine. It is deliberately dumb:
// word-by-word lookup, unknown words pass through unchanged.
import { describe, it, expect } from 'vitest';
import { dictionaryEngine } from './dictionary';

describe('dictionaryEngine', () => {
  it('converts a known everyday sentence', async () => {
    const r = await dictionaryEngine.convert('kya haal hai');
    expect(r).toEqual({ ok: true, urdu: 'کیا حال ہے' });
  });

  it('passes unknown words through unchanged', async () => {
    const r = await dictionaryEngine.convert('kya zeitgeist hai');
    expect(r).toEqual({ ok: true, urdu: 'کیا zeitgeist ہے' });
  });

  it('normalizes case and strips punctuation for lookup', async () => {
    const r = await dictionaryEngine.convert('Kya haal hai?');
    expect(r.ok).toBe(true);
    if (r.ok) expect(r.urdu).toContain('کیا');
  });

  it('rejects input with zero recognizable Urdu words', async () => {
    const r = await dictionaryEngine.convert('the quick brown fox');
    expect(r).toEqual({ ok: false, reason: 'not_roman_urdu' });
  });

  it('rejects empty input', async () => {
    const r = await dictionaryEngine.convert('   ');
    expect(r).toEqual({ ok: false, reason: 'not_roman_urdu' });
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run src/lib/engines/dictionary.test.ts`
Expected: FAIL — cannot resolve `./dictionary`.

- [ ] **Step 3: Write types + starter dictionary data**

`src/lib/engines/types.ts`:

```ts
// The contract every conversion engine signs. The app, the API route, and
// the eval harness all talk to THIS, never to a specific engine.
// CONCEPT: strategy pattern — swappable implementations behind one interface;
// the naive alternative is if/else branches on engine name scattered everywhere.
export type ConversionResult =
  | { ok: true; urdu: string }
  | { ok: false; reason: 'not_roman_urdu' | 'engine_error' };

export interface ConversionEngine {
  name: string;
  convert(roman: string): Promise<ConversionResult>;
}
```

`src/lib/engines/dictionary-data.ts` — starter map (Step 6 expands it to ~500):

```ts
// Roman Urdu → Urdu script word mappings for the baseline engine.
// Keys are lowercase, punctuation-stripped. Include common variant spellings
// as separate keys ("nahi", "nhi", "nai" all map to نہیں).
export const WORD_MAP: Record<string, string> = {
  kya: 'کیا', kia: 'کیا', haal: 'حال', hal: 'حال', hai: 'ہے', he: 'ہے',
  hain: 'ہیں', hun: 'ہوں', hoon: 'ہوں', ho: 'ہو', tha: 'تھا', thi: 'تھی', thay: 'تھے',
  nahi: 'نہیں', nhi: 'نہیں', nai: 'نہیں', nahin: 'نہیں', na: 'نہ',
  main: 'میں', mein: 'میں', mai: 'میں', mujhe: 'مجھے', mujhay: 'مجھے', mera: 'میرا',
  meri: 'میری', mere: 'میرے', hum: 'ہم', hamara: 'ہمارا', tum: 'تم', tumhara: 'تمہارا',
  aap: 'آپ', ap: 'آپ', apka: 'آپ کا', wo: 'وہ', woh: 'وہ', ye: 'یہ', yeh: 'یہ',
  is: 'اس', us: 'اس', iska: 'اس کا', uska: 'اس کا',
  aur: 'اور', or: 'اور', ya: 'یا', lekin: 'لیکن', magar: 'مگر', to: 'تو', tou: 'تو',
  bhi: 'بھی', sirf: 'صرف', bas: 'بس', ab: 'اب', phir: 'پھر', fir: 'پھر',
  kal: 'کل', aj: 'آج', aaj: 'آج', raat: 'رات', din: 'دن', subah: 'صبح', shaam: 'شام',
  zindagi: 'زندگی', dil: 'دل', pyar: 'پیار', pyaar: 'پیار', mohabbat: 'محبت',
  dost: 'دوست', yaar: 'یار', yar: 'یار', bhai: 'بھائی', maa: 'ماں', maan: 'ماں',
  ghar: 'گھر', kaam: 'کام', kam: 'کام', paisa: 'پیسہ', paise: 'پیسے',
  acha: 'اچھا', accha: 'اچھا', achi: 'اچھی', bura: 'برا', bara: 'بڑا', bari: 'بڑی',
  chota: 'چھوٹا', naya: 'نیا', purana: 'پرانا',
  karna: 'کرنا', karo: 'کرو', kar: 'کر', karta: 'کرتا', karti: 'کرتی', kiya: 'کیا',
  hona: 'ہونا', hota: 'ہوتا', hoti: 'ہوتی', gaya: 'گیا', gayi: 'گئی', gaye: 'گئے',
  jana: 'جانا', jao: 'جاؤ', aana: 'آنا', aao: 'آؤ', aya: 'آیا', ayi: 'آئی',
  dekh: 'دیکھ', dekho: 'دیکھو', dekhna: 'دیکھنا', suno: 'سنو', bolo: 'بولو',
  khana: 'کھانا', pani: 'پانی', chai: 'چائے', chalo: 'چلو', chal: 'چل',
  kuch: 'کچھ', kuchh: 'کچھ', sab: 'سب', bohat: 'بہت', bahut: 'بہت', bht: 'بہت',
  thora: 'تھوڑا', zyada: 'زیادہ', ziada: 'زیادہ',
  kyun: 'کیوں', kyu: 'کیوں', kaise: 'کیسے', kese: 'کیسے', kahan: 'کہاں', kab: 'کب',
  kaun: 'کون', kon: 'کون', jab: 'جب', tab: 'تب', agar: 'اگر', ager: 'اگر',
  shukriya: 'شکریہ', salam: 'سلام', assalam: 'السلام', khuda: 'خدا', allah: 'اللہ',
  hafiz: 'حافظ', inshallah: 'انشاءاللہ', mashallah: 'ماشاءاللہ',
  se: 'سے', ka: 'کا', ki: 'کی', ke: 'کے', ko: 'کو', par: 'پر', pe: 'پہ', tak: 'تک',
  liye: 'لیے', lie: 'لیے', wala: 'والا', wali: 'والی', saath: 'ساتھ', sath: 'ساتھ',
  baat: 'بات', bat: 'بات', log: 'لوگ', duniya: 'دنیا', waqt: 'وقت', dua: 'دعا',
  jannat: 'جنت', hawa: 'ہوا', truck: 'ٹرک', gaari: 'گاڑی', gari: 'گاڑی',
  sona: 'سونا', neend: 'نیند', khwab: 'خواب', sapna: 'سپنا',
};
```

- [ ] **Step 4: Write the engine**

`src/lib/engines/dictionary.ts`:

```ts
// The baseline engine: word-by-word dictionary lookup, longest effort none.
// It exists to LOSE — it makes the Claude engine's benchmark score meaningful.
import type { ConversionEngine, ConversionResult } from './types';
import { WORD_MAP } from './dictionary-data';

// Lookup keys are lowercase with punctuation stripped; apostrophes kept ("ma'an").
function normalizeToken(token: string): string {
  return token.toLowerCase().replace(/[^a-z']/g, '');
}

export const dictionaryEngine: ConversionEngine = {
  name: 'dictionary',
  async convert(roman: string): Promise<ConversionResult> {
    const trimmed = roman.trim();
    if (!trimmed) return { ok: false, reason: 'not_roman_urdu' };

    let matched = 0;
    const words = trimmed.split(/\s+/).map((token) => {
      const hit = WORD_MAP[normalizeToken(token)];
      if (hit) {
        matched++;
        return hit;
      }
      return token; // unknown words pass through — an honest baseline doesn't guess
    });

    if (matched === 0) return { ok: false, reason: 'not_roman_urdu' };
    return { ok: true, urdu: words.join(' ') };
  },
};
```

- [ ] **Step 5: Run tests to verify they pass**

Run: `npx vitest run src/lib/engines/dictionary.test.ts`
Expected: 5 passed. Note: `'Kya haal hai?'` → `hai?` normalizes to `hai` → `ہے`, so the contains-check passes.

- [ ] **Step 6: Expand dictionary to ~500 entries**

Extend `WORD_MAP` to roughly 500 keys following the exact existing pattern. Generate the additions yourself (you are a capable Urdu-aware model): the ~300 most common conversational Roman Urdu words plus their frequent variant spellings — pronouns, verb forms (past/present/imperative of کرنا، ہونا، جانا، آنا، دینا، لینا، کہنا، ملنا), numbers 1–20, days, family terms, common adjectives/adverbs, WhatsApp shorthand (`h` → `ہے`, `q` → 'کیوں', `pta` → 'پتہ'). Keep keys lowercase; duplicates are a bug (TypeScript flags duplicate literal keys). Sanity-check: `npx vitest run` still green.

- [ ] **Step 7: Commit**

```bash
git add src/lib/engines && git commit -m "feat: engine interface + dictionary baseline engine"
```

---

### Task 3: Claude engine (TDD, dependency-injected client)

**Files:**
- Create: `src/lib/engines/claude.ts`
- Test: `src/lib/engines/claude.test.ts`

**Interfaces:**
- Consumes: `ConversionEngine`, `ConversionResult` from `src/lib/engines/types.ts`.
- Produces (used by Tasks 4, 11):

```ts
export type MessagesClient = {
  messages: { create(params: Record<string, unknown>): Promise<{ content: Array<{ type: string; text?: string }> }> };
};
export function createClaudeEngine(client: MessagesClient): ConversionEngine;
export function claudeEngineFromKey(apiKey: string): ConversionEngine; // wraps real Anthropic SDK
```

- [ ] **Step 1: Write the failing test**

`src/lib/engines/claude.test.ts`:

```ts
// CONCEPT: dependency injection — the engine receives its API client instead of
// creating one, so tests hand it a fake and never touch the network or spend money.
import { describe, it, expect } from 'vitest';
import { createClaudeEngine, type MessagesClient } from './claude';

function fakeClient(reply: string | Error): MessagesClient {
  return {
    messages: {
      async create() {
        if (reply instanceof Error) throw reply;
        return { content: [{ type: 'text', text: reply }] };
      },
    },
  };
}

describe('createClaudeEngine', () => {
  it('returns urdu when Claude replies with ok JSON', async () => {
    const engine = createClaudeEngine(fakeClient('{"ok": true, "urdu": "کیا حال ہے"}'));
    expect(await engine.convert('kya haal hai')).toEqual({ ok: true, urdu: 'کیا حال ہے' });
  });

  it('maps a not_roman_urdu reply through', async () => {
    const engine = createClaudeEngine(fakeClient('{"ok": false, "reason": "not_roman_urdu"}'));
    expect(await engine.convert('asdfgh')).toEqual({ ok: false, reason: 'not_roman_urdu' });
  });

  it('returns engine_error on malformed JSON', async () => {
    const engine = createClaudeEngine(fakeClient('Sure! Here is the conversion:')); 
    expect(await engine.convert('kya haal hai')).toEqual({ ok: false, reason: 'engine_error' });
  });

  it('returns engine_error when the API throws', async () => {
    const engine = createClaudeEngine(fakeClient(new Error('529 overloaded')));
    expect(await engine.convert('kya haal hai')).toEqual({ ok: false, reason: 'engine_error' });
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run src/lib/engines/claude.test.ts`
Expected: FAIL — cannot resolve `./claude`.

- [ ] **Step 3: Write the engine**

`src/lib/engines/claude.ts`:

```ts
// The real engine: prompt-engineered Claude call that transliterates AND
// normalizes grammar/spelling. The prompt is the product here — this is the
// FDE craft the benchmark measures.
import Anthropic from '@anthropic-ai/sdk';
import type { ConversionEngine, ConversionResult } from './types';

export const SYSTEM_PROMPT = `You convert Roman Urdu (Urdu typed in Latin letters) into correct Urdu script.

Rules:
- Reply with strict JSON only, no prose: {"ok": true, "urdu": "..."} or {"ok": false, "reason": "not_roman_urdu"}.
- Transliterate into Urdu script and fix spelling/grammar so it reads as natural written Urdu.
- Roman Urdu spelling is chaotic: "nahi", "nhi", "nai" are all نہیں. Resolve by context and meaning.
- Code-switched English words Urdu speakers use ("meeting", "office") become their standard Urdu transliteration (میٹنگ، آفس).
- Preserve meaning and tone exactly. Never add, remove, or translate content.
- If the input is not Roman Urdu (gibberish, pure English prose, another language), return {"ok": false, "reason": "not_roman_urdu"}.`;

export type MessagesClient = {
  messages: {
    create(params: Record<string, unknown>): Promise<{ content: Array<{ type: string; text?: string }> }>;
  };
};

export function createClaudeEngine(client: MessagesClient): ConversionEngine {
  return {
    name: 'claude',
    async convert(roman: string): Promise<ConversionResult> {
      try {
        const msg = await client.messages.create({
          model: 'claude-sonnet-5',
          max_tokens: 1000,
          system: SYSTEM_PROMPT,
          messages: [{ role: 'user', content: roman }],
        });
        const block = msg.content[0];
        const parsed = JSON.parse(block?.type === 'text' && block.text ? block.text : '');
        if (parsed.ok === true && typeof parsed.urdu === 'string') return { ok: true, urdu: parsed.urdu };
        if (parsed.ok === false && parsed.reason === 'not_roman_urdu') return { ok: false, reason: 'not_roman_urdu' };
        return { ok: false, reason: 'engine_error' };
      } catch {
        return { ok: false, reason: 'engine_error' };
      }
    },
  };
}

// Production wiring: real SDK client from an API key (key comes from the server route's env).
export function claudeEngineFromKey(apiKey: string): ConversionEngine {
  return createClaudeEngine(new Anthropic({ apiKey }) as unknown as MessagesClient);
}
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `npx vitest run src/lib/engines/claude.test.ts`
Expected: 4 passed.

- [ ] **Step 5: One real smoke call (spends ~a cent)**

```bash
ANTHROPIC_API_KEY=$(grep ANTHROPIC_API_KEY .env | cut -d= -f2) npx tsx -e "
import { claudeEngineFromKey } from './src/lib/engines/claude.ts';
claudeEngineFromKey(process.env.ANTHROPIC_API_KEY!).convert('zindagi mein kuch karna hai to').then(console.log);
"
```

Expected: `{ ok: true, urdu: 'زندگی میں کچھ کرنا ہے تو' }` (wording may vary slightly; must be valid Urdu script). Requires `.env` with a real key — if absent, ask Moiz for it, don't fake this step.

- [ ] **Step 6: Commit**

```bash
git add src/lib/engines && git commit -m "feat: Claude conversion engine with injected client"
```

---

### Task 4: `/api/convert` server route (validation, rate limit, fail-closed)

**Files:**
- Create: `src/lib/server/rate-limit.ts`
- Test: `src/lib/server/rate-limit.test.ts`
- Create: `src/routes/api/convert/+server.ts`

**Interfaces:**
- Consumes: `claudeEngineFromKey` (Task 3).
- Produces (used by Task 6 UI): `POST /api/convert` with body `{ text: string }` →
  - 200 `{ ok: true, urdu }`
  - 422 `{ ok: false, reason: 'not_roman_urdu' | 'engine_error' }`
  - 400 `{ ok: false, reason: 'invalid_input' }`
  - 429 `{ ok: false, reason: 'rate_limited' }`
  - 500 `{ ok: false, reason: 'engine_error' }` (missing key — fail closed)
- Produces (used by Task 9): `checkRateLimit(ip: string, now?: number): boolean`.

- [ ] **Step 1: Write the failing rate-limit test**

`src/lib/server/rate-limit.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { checkRateLimit } from './rate-limit';

describe('checkRateLimit', () => {
  it('allows 10 requests then blocks the 11th within a minute', () => {
    const ip = 'test-ip-1';
    for (let i = 0; i < 10; i++) expect(checkRateLimit(ip, 1000 + i)).toBe(true);
    expect(checkRateLimit(ip, 1011)).toBe(false);
  });

  it('frees the window after 60s', () => {
    const ip = 'test-ip-2';
    for (let i = 0; i < 10; i++) checkRateLimit(ip, 1000 + i);
    expect(checkRateLimit(ip, 1000 + 60_001)).toBe(true);
  });

  it('tracks IPs independently', () => {
    for (let i = 0; i < 10; i++) checkRateLimit('ip-a', 1000 + i);
    expect(checkRateLimit('ip-b', 1010)).toBe(true);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run src/lib/server/rate-limit.test.ts` — Expected: FAIL, module not found.

- [ ] **Step 3: Implement rate limiter**

`src/lib/server/rate-limit.ts`:

```ts
// Per-IP sliding-window rate limit for endpoints that spend money.
// In-memory: resets when the serverless instance recycles. That's acceptable —
// it caps burst abuse per instance without needing a database. The naive
// alternative (no limit) means one bored script kiddie drains the API budget.
const WINDOW_MS = 60_000;
const MAX_REQUESTS = 10;
const hits = new Map<string, number[]>();

export function checkRateLimit(ip: string, now: number = Date.now()): boolean {
  const recent = (hits.get(ip) ?? []).filter((t) => now - t < WINDOW_MS);
  if (recent.length >= MAX_REQUESTS) {
    hits.set(ip, recent);
    return false;
  }
  recent.push(now);
  hits.set(ip, recent);
  return true;
}
```

- [ ] **Step 4: Run tests** — `npx vitest run src/lib/server/rate-limit.test.ts` — Expected: 3 passed.

- [ ] **Step 5: Write the route**

`src/routes/api/convert/+server.ts`:

```ts
// Server-only conversion endpoint. The Anthropic key lives here and NEVER
// reaches the browser. CONCEPT: API route — code that runs on the server;
// the browser can call it but can't read its source or its secrets.
import { json } from '@sveltejs/kit';
import { env } from '$env/dynamic/private';
import type { RequestHandler } from './$types';
import { claudeEngineFromKey } from '$lib/engines/claude';
import { checkRateLimit } from '$lib/server/rate-limit';

const MAX_INPUT_CHARS = 500;

export const POST: RequestHandler = async ({ request, getClientAddress }) => {
  // Fail closed: no key means no service — never a degraded fake answer.
  if (!env.ANTHROPIC_API_KEY) {
    return json({ ok: false, reason: 'engine_error' }, { status: 500 });
  }
  if (!checkRateLimit(getClientAddress())) {
    return json({ ok: false, reason: 'rate_limited' }, { status: 429 });
  }

  const body = await request.json().catch(() => null);
  const text = typeof body?.text === 'string' ? body.text.trim() : '';
  if (!text || text.length > MAX_INPUT_CHARS) {
    return json({ ok: false, reason: 'invalid_input' }, { status: 400 });
  }

  const result = await claudeEngineFromKey(env.ANTHROPIC_API_KEY).convert(text);
  return json(result, { status: result.ok ? 200 : 422 });
};
```

- [ ] **Step 6: Verify manually against the dev server**

```bash
npm run dev -- --port 5173 &
sleep 3
curl -s -X POST localhost:5173/api/convert -H 'content-type: application/json' -d '{"text":"kya haal hai"}'
curl -s -X POST localhost:5173/api/convert -H 'content-type: application/json' -d '{"text":""}'
curl -s -X POST localhost:5173/api/convert -H 'content-type: application/json' -d 'not json'
```

Expected: `{"ok":true,"urdu":"کیا حال ہے"}` (needs `.env` key; SvelteKit loads `.env` automatically), then `{"ok":false,"reason":"invalid_input"}` twice.

- [ ] **Step 7: Commit**

```bash
git add src/lib/server src/routes/api/convert && git commit -m "feat: /api/convert route with rate limit and fail-closed key handling"
```

---

### Task 5: Truck-art design system (tokens, fonts, SVG chrome)

**Files:**
- Create: `src/lib/styles/tokens.css`
- Create: `src/routes/+layout.svelte`
- Create: `src/lib/components/PhoolPattiFrame.svelte`
- Create: `src/lib/components/ChamakDivider.svelte`

**Interfaces:**
- Consumes: fontsource packages (Task 1).
- Produces (used by Tasks 6, 8, 12): CSS custom properties (`--lacquer`, `--emerald`, `--canary`, `--rose-red`, `--electric-blue`, `--magenta`, `--bone`, `--ink`), utility classes `.urdu` and `.latin`, components `<PhoolPattiFrame>` (slot wrapper) and `<ChamakDivider>`.

- [ ] **Step 1: Invoke the frontend-design skill FIRST** (Moiz's CLAUDE.md requires it before UI code). Bring the spec's "Visual identity" section as the brief: panelized phool patti frames, chamak patti accents, lacquered midnight ground, truck-palette with white/black outlines. The skill's output refines the exact values below — treat the code here as the starting point, not the ceiling.

- [ ] **Step 2: Write tokens + global styles**

`src/lib/styles/tokens.css`:

```css
/* Likhai design tokens — the truck's paint shop.
   Truck palette: saturated hues ALWAYS separated by bone-white or ink outlines,
   which is how real phool patti stays graphic instead of muddy. */
:root {
  --lacquer: #101636;      /* midnight cab lacquer — the app's ground */
  --lacquer-deep: #0a0e24;
  --emerald: #00a651;
  --canary: #ffd100;
  --rose-red: #e4002b;
  --electric-blue: #00a3e0;
  --magenta: #e5007d;
  --bone: #fff8ea;         /* outline white, warm not clinical */
  --ink: #14100c;

  --font-urdu: 'Noto Nastaliq Urdu', serif;
  --font-latin: 'Bricolage Grotesque', sans-serif;
}

body {
  background: var(--lacquer);
  color: var(--bone);
  font-family: var(--font-latin);
  margin: 0;
}

.urdu {
  font-family: var(--font-urdu);
  direction: rtl;      /* CONCEPT: RTL — Urdu reads right-to-left; the browser
                          handles letter joining and direction when told */
  line-height: 2.1;    /* Nastaliq hangs deep below the baseline; give it room */
}

.latin { font-family: var(--font-latin); }
```

`src/routes/+layout.svelte`:

```svelte
<script lang="ts">
  // Root layout: fonts + tokens load once here and wrap every page.
  import '@fontsource/noto-nastaliq-urdu';
  import '@fontsource/bricolage-grotesque/400.css';
  import '@fontsource/bricolage-grotesque/700.css';
  import '$lib/styles/tokens.css';
  let { children } = $props();
</script>

{@render children()}
```

- [ ] **Step 3: Build the SVG chrome components**

`src/lib/components/PhoolPattiFrame.svelte` — the panelized border every content block sits in:

```svelte
<script lang="ts">
  // Every surface on a truck is a bordered panel. This is the app's version:
  // a double border with scalloped SVG edging, recolorable via props.
  let { accent = 'var(--canary)', children } = $props();
</script>

<div class="frame" style:--accent={accent}>
  <svg class="scallops" aria-hidden="true" preserveAspectRatio="none" viewBox="0 0 400 12">
    {#each Array.from({ length: 20 }) as _, i}
      <circle cx={10 + i * 20} cy="0" r="7" fill="var(--accent)" stroke="var(--bone)" stroke-width="1.5" />
    {/each}
  </svg>
  <div class="inner">{@render children()}</div>
</div>

<style>
  .frame {
    border: 3px solid var(--bone);
    outline: 3px solid var(--accent);
    outline-offset: 3px;
    border-radius: 6px;
    background: var(--lacquer-deep);
    position: relative;
    padding: 2rem 1.5rem 1.5rem;
  }
  .scallops { position: absolute; top: -3px; left: 0; width: 100%; height: 12px; }
  .inner { position: relative; }
</style>
```

`src/lib/components/ChamakDivider.svelte` — Karachi tape-work as a section divider:

```svelte
<script lang="ts">
  let { height = 14 } = $props();
</script>

<!-- Chamak patti: alternating reflective-tape triangles. -->
<svg width="100%" {height} aria-hidden="true" preserveAspectRatio="none" viewBox="0 0 560 14">
  {#each Array.from({ length: 28 }) as _, i}
    <polygon
      points="{i * 20},14 {i * 20 + 10},0 {i * 20 + 20},14"
      fill={['var(--rose-red)', 'var(--canary)', 'var(--emerald)', 'var(--electric-blue)'][i % 4]}
      stroke="var(--ink)" stroke-width="1"
    />
  {/each}
</svg>
```

- [ ] **Step 4: Visual check** — temporary usage on the home page, `npm run dev`, screenshot with the webapp-testing skill's browser tools, and eyeball: outlines crisp, scallops aligned, palette right. Iterate until it looks like a truck, not a bootstrap theme.

- [ ] **Step 5: Commit**

```bash
git add src/lib/styles src/lib/components src/routes/+layout.svelte && git commit -m "feat: truck-art design system (tokens, fonts, phool patti + chamak patti chrome)"
```

---

### Task 6: Converter page (debounced live conversion)

**Files:**
- Create: `src/routes/+page.svelte` (replace scaffold placeholder)

**Interfaces:**
- Consumes: `POST /api/convert` (Task 4), design system (Task 5).
- Produces: working converter UI; exposes `urdu` result + "Truck pe laga do" button that Task 8 wires to the card studio; consent checkbox state that Task 9 wires to the corpus.

- [ ] **Step 1: Build the page**

`src/routes/+page.svelte`:

```svelte
<script lang="ts">
  // Likhai's main page: type Roman Urdu, watch real Urdu fade in.
  // CONCEPT: reactive state — `$state` variables re-render the page when they
  // change; Svelte compiles this away instead of running a diffing engine.
  import PhoolPattiFrame from '$lib/components/PhoolPattiFrame.svelte';
  import ChamakDivider from '$lib/components/ChamakDivider.svelte';
  import { fade } from 'svelte/transition';

  let roman = $state('');
  let urdu = $state('');
  let status = $state<'idle' | 'loading' | 'done' | 'error' | 'not_urdu'>('idle');
  let consent = $state(false);
  let copied = $state(false);
  let timer: ReturnType<typeof setTimeout> | undefined;

  // CONCEPT: debounce — wait until the user pauses typing (800ms) before
  // calling the paid API, instead of firing on every keystroke.
  function onInput() {
    clearTimeout(timer);
    const text = roman.trim();
    if (!text) {
      status = 'idle';
      urdu = '';
      return;
    }
    timer = setTimeout(() => convert(text), 800);
  }

  async function convert(text: string) {
    status = 'loading';
    try {
      const res = await fetch('/api/convert', {
        method: 'POST',
        headers: { 'content-type': 'application/json' },
        body: JSON.stringify({ text }),
      });
      const data = await res.json();
      if (res.ok && data.ok) {
        urdu = data.urdu;
        status = 'done';
      } else if (data.reason === 'not_roman_urdu') {
        status = 'not_urdu';
      } else {
        status = 'error';
      }
    } catch {
      status = 'error';
    }
  }

  async function copyUrdu() {
    await navigator.clipboard.writeText(urdu);
    copied = true;
    setTimeout(() => (copied = false), 1500);
  }
</script>

<main>
  <header>
    <h1 class="urdu">لکھائی</h1>
    <p class="latin tagline">Roman Urdu in. Real Urdu out.</p>
  </header>

  <ChamakDivider />

  <PhoolPattiFrame accent="var(--canary)">
    <label class="latin" for="roman-input">Likho — type Roman Urdu</label>
    <textarea
      id="roman-input"
      bind:value={roman}
      oninput={onInput}
      maxlength="500"
      rows="3"
      placeholder="kya haal hai yaar..."
    ></textarea>
  </PhoolPattiFrame>

  {#if status === 'loading'}
    <p class="latin status" transition:fade>painting…</p>
  {:else if status === 'done'}
    <PhoolPattiFrame accent="var(--emerald)">
      <p class="urdu result" transition:fade>{urdu}</p>
      <div class="actions">
        <button class="latin" onclick={copyUrdu}>{copied ? 'Copied!' : 'Copy'}</button>
        <button class="latin primary">ٹرک پہ لگا دو</button>
      </div>
    </PhoolPattiFrame>
    <label class="latin consent">
      <input type="checkbox" bind:checked={consent} />
      Add this conversion (anonymously) to an open Urdu dataset
    </label>
  {:else if status === 'not_urdu'}
    <p class="latin status" transition:fade>Yeh Roman Urdu nahi lag rahi — try something like "dil ki baat"</p>
  {:else if status === 'error'}
    <p class="latin status error" transition:fade>
      Kuch garbar ho gayi. <button class="latin" onclick={() => convert(roman.trim())}>Try again</button>
    </p>
  {/if}
</main>

<style>
  main { max-width: 640px; margin: 0 auto; padding: 3rem 1.25rem; display: grid; gap: 1.75rem; }
  h1 { font-size: 3.5rem; margin: 0; text-align: center; color: var(--canary); }
  .tagline { text-align: center; letter-spacing: 0.06em; }
  textarea {
    width: 100%; box-sizing: border-box; font-family: var(--font-latin); font-size: 1.15rem;
    background: transparent; color: var(--bone); border: none; outline: none; resize: vertical;
  }
  .result { font-size: 2rem; text-align: center; margin: 0.5rem 0 1rem; }
  .actions { display: flex; gap: 0.75rem; justify-content: center; }
  button {
    cursor: pointer; border: 2px solid var(--bone); border-radius: 4px;
    background: var(--lacquer-deep); color: var(--bone); padding: 0.5rem 1.25rem; font-size: 1rem;
  }
  button.primary { background: var(--rose-red); border-color: var(--canary); font-weight: 700; }
  button:focus-visible { outline: 3px solid var(--electric-blue); outline-offset: 2px; }
  .consent { display: flex; gap: 0.5rem; align-items: center; font-size: 0.9rem; opacity: 0.85; }
  .status { text-align: center; }
  .error { color: var(--canary); }
</style>
```

Note: the "ٹرک پہ لگا دو" button is intentionally inert until Task 8; `consent` is intentionally unused until Task 9.

- [ ] **Step 2: Verify in the browser** — `npm run dev`, type `kya haal hai yaar`, confirm: debounce (one network call), Urdu fades in, Copy works, empty input returns to idle, killing the network (devtools offline) shows the retry error state.

- [ ] **Step 3: Run the web-interface-guidelines skill** on `src/routes/+page.svelte` and fix what it flags (focus states, labels, contrast, mobile).

- [ ] **Step 4: Commit**

```bash
git add src/routes/+page.svelte && git commit -m "feat: converter page with debounced live conversion"
```

---

### Task 7: AI art assets (generate once, curate with Moiz)

**Files:**
- Create: `static/art/truck-full.png` (the hero truck-back illustration, tailgate deliberately blank)
- Create: `static/art/theme-gulab.png`, `theme-morr.png`, `theme-chamak.png`, `theme-raat.png` (card backgrounds, ~1200×1500, each with a clear central panel left uncluttered for text)
- Create: `src/lib/themes.ts`

**Interfaces:**
- Consumes: nothing from code.
- Produces (used by Task 8):

```ts
export type CardTheme = {
  id: string;
  label: string;          // display name, e.g. "Gulab · گلاب"
  background: string;     // "/art/theme-gulab.png"
  textColor: string;      // CSS color for the Nastaliq overlay
  shadowColor: string;    // text-shadow color for painted-letter depth
};
export const CARD_THEMES: CardTheme[];
```

- [ ] **Step 1: Generate candidates with an image model** (Higgsfield MCP `generate_image`, or ask Moiz's preference if unavailable). Four prompts, 2–3 candidates each. Base prompt language:

> "Pakistani truck art phool patti painting, [SUBJECT], saturated emerald green, canary yellow, scarlet red, electric blue on deep midnight lacquer, bold white outlines around every shape, ornate nested panel borders with scalloped edges, flat decorative folk-painting style, symmetrical composition, a large EMPTY plain dark central panel with ornate border reserved for text, no letters, no words, no writing anywhere"

Subjects: (a) full rear view of a decorated Bedford truck, tailgate blank; (b) giant stylized roses and jasmine garlands («gulab»); (c) a fanned peacock («morr»); (d) chamak patti geometric mirror-tape mosaic; (e) night truck with rainbow lightbulb strings («raat»). **"No text/letters anywhere" matters** — models love garbling fake Urdu.

- [ ] **Step 2: Curation checkpoint with Moiz.** Show candidates, let him pick winners (this is his brand). Do not proceed on your own taste.

- [ ] **Step 3: Save winners** to `static/art/` with the exact filenames above (downscale to ≤1600px longest side, target <400KB each — `sips -Z 1600` on macOS).

- [ ] **Step 4: Write theme metadata**

`src/lib/themes.ts`:

```ts
// Card themes: each pairs a pre-generated painting with text colors tuned to it.
// One source of truth — the studio, previews, and exports all read this list.
export type CardTheme = {
  id: string;
  label: string;
  background: string;
  textColor: string;
  shadowColor: string;
};

export const CARD_THEMES: CardTheme[] = [
  { id: 'truck',  label: 'Truck · ٹرک',    background: '/art/truck-full.png',  textColor: '#fff8ea', shadowColor: '#14100c' },
  { id: 'gulab',  label: 'Gulab · گلاب',   background: '/art/theme-gulab.png', textColor: '#ffd100', shadowColor: '#7a0016' },
  { id: 'morr',   label: 'Morr · مور',     background: '/art/theme-morr.png',  textColor: '#fff8ea', shadowColor: '#003c2a' },
  { id: 'chamak', label: 'Chamak · چمک',   background: '/art/theme-chamak.png', textColor: '#14100c', shadowColor: '#fff8ea' },
  { id: 'raat',   label: 'Raat · رات',     background: '/art/theme-raat.png',  textColor: '#ffd100', shadowColor: '#000000' },
];
```

(Adjust `textColor`/`shadowColor` per winning image for contrast — check visually in Task 8.)

- [ ] **Step 5: Commit**

```bash
git add static/art src/lib/themes.ts && git commit -m "feat: curated truck-art asset library + theme metadata"
```

---

### Task 8: Card studio ("Truck pe laga do")

**Files:**
- Create: `src/lib/components/CardStudio.svelte`
- Modify: `src/routes/+page.svelte` (wire the primary button)

**Interfaces:**
- Consumes: `CARD_THEMES`, `CardTheme` (Task 7); `html-to-image` (Task 1).
- Produces: `<CardStudio urdu={string} onclose={() => void} />` — full-screen overlay, theme picker, PNG download.

- [ ] **Step 1: Build the component**

`src/lib/components/CardStudio.svelte`:

```svelte
<script lang="ts">
  // The payoff screen: the user's Urdu line on a truck. DOM (real Nastaliq
  // font) layered over a pre-generated painting, snapshotted to PNG.
  // CONCEPT: DOM-to-image — html-to-image draws the live DOM onto a canvas;
  // we use it because the browser is the only renderer that shapes Nastaliq right.
  import { toPng } from 'html-to-image';
  import { CARD_THEMES } from '$lib/themes';
  import { fade } from 'svelte/transition';

  let { urdu, onclose } = $props<{ urdu: string; onclose: () => void }>();

  let theme = $state(CARD_THEMES[0]);
  let cardEl: HTMLDivElement;
  let downloading = $state(false);

  async function download() {
    downloading = true;
    try {
      // pixelRatio 2 → crisp on retina + WhatsApp compression
      const dataUrl = await toPng(cardEl, { pixelRatio: 2 });
      const a = document.createElement('a');
      a.download = `likhai-${theme.id}.png`;
      a.href = dataUrl;
      a.click();
    } finally {
      downloading = false;
    }
  }
</script>

<div class="overlay" transition:fade role="dialog" aria-modal="true" aria-label="Card studio">
  <div class="studio">
    <div class="card" bind:this={cardEl} style:background-image="url({theme.background})">
      <p class="urdu line" style:color={theme.textColor} style:--shadow={theme.shadowColor}>{urdu}</p>
      <span class="latin watermark">likhai</span>
    </div>

    <div class="themes" role="radiogroup" aria-label="Art theme">
      {#each CARD_THEMES as t}
        <button
          class="latin theme-btn"
          class:active={t.id === theme.id}
          role="radio"
          aria-checked={t.id === theme.id}
          onclick={() => (theme = t)}
        >{t.label}</button>
      {/each}
    </div>

    <div class="actions">
      <button class="latin primary" onclick={download} disabled={downloading}>
        {downloading ? 'Painting…' : 'Download PNG'}
      </button>
      <button class="latin" onclick={onclose}>Close</button>
    </div>
  </div>
</div>

<style>
  .overlay {
    position: fixed; inset: 0; background: rgb(10 14 36 / 0.92);
    display: grid; place-items: center; padding: 1rem; z-index: 10;
  }
  .studio { display: grid; gap: 1rem; max-width: 420px; width: 100%; }
  .card {
    aspect-ratio: 4 / 5; background-size: cover; background-position: center;
    border: 4px solid var(--bone); border-radius: 8px;
    display: grid; place-items: center; padding: 2rem; position: relative;
  }
  .line {
    font-size: clamp(1.4rem, 6vw, 2.4rem); text-align: center; margin: 0;
    text-shadow: 2px 2px 0 var(--shadow), -1px -1px 0 var(--shadow); /* painted-letter depth */
  }
  .watermark { position: absolute; bottom: 0.6rem; right: 0.9rem; font-size: 0.75rem; opacity: 0.7; color: var(--bone); }
  .themes { display: flex; gap: 0.5rem; flex-wrap: wrap; justify-content: center; }
  .theme-btn { border: 2px solid var(--bone); background: transparent; color: var(--bone); border-radius: 4px; padding: 0.35rem 0.75rem; cursor: pointer; }
  .theme-btn.active { background: var(--canary); color: var(--ink); border-color: var(--canary); }
  .actions { display: flex; gap: 0.75rem; justify-content: center; }
  button.primary { background: var(--rose-red); border: 2px solid var(--canary); color: var(--bone); font-weight: 700; padding: 0.6rem 1.5rem; border-radius: 4px; cursor: pointer; }
  button:focus-visible { outline: 3px solid var(--electric-blue); outline-offset: 2px; }
</style>
```

- [ ] **Step 2: Wire it into the page.** In `src/routes/+page.svelte`: add `import CardStudio from '$lib/components/CardStudio.svelte';`, add `let showStudio = $state(false);`, change the primary button to `<button class="latin primary" onclick={() => (showStudio = true)}>ٹرک پہ لگا دو</button>`, and after `</main>` add:

```svelte
{#if showStudio}
  <CardStudio {urdu} onclose={() => (showStudio = false)} />
{/if}
```

- [ ] **Step 3: Verify in the browser** — convert a line, open the studio, switch all themes (text stays legible on each; adjust `textColor`/`shadowColor` in `themes.ts` where it doesn't), download PNG, **open the PNG** and confirm the Nastaliq shaped correctly and the watermark is there. Test a long line (~200 chars) — `clamp()` should keep it inside the card.

- [ ] **Step 4: Run web-interface-guidelines** on the new component; fix findings (dialog focus, Escape-to-close is a worthwhile addition).

- [ ] **Step 5: Commit**

```bash
git add src/lib/components/CardStudio.svelte src/routes/+page.svelte src/lib/themes.ts
git commit -m "feat: card studio — truck pe laga do"
```

---

### Task 9: Corpus (Turso + consent wiring)

**Files:**
- Create: `src/lib/server/db.ts`
- Create: `scripts/migrate.ts`
- Create: `src/routes/api/corpus/+server.ts`
- Modify: `src/routes/+page.svelte` (fire corpus save when consented)

**Interfaces:**
- Consumes: `checkRateLimit` (Task 4), env vars `TURSO_DATABASE_URL` / `TURSO_AUTH_TOKEN`.
- Produces: `POST /api/corpus` body `{ roman: string, urdu: string, engine: string }` → 201 `{ ok: true }` | 400 | 429 | 503 (db not configured). Table `corpus_pairs(id INTEGER PK, roman_text TEXT, urdu_text TEXT, engine TEXT, created_at TEXT)`.

- [ ] **Step 1: Human step — Turso account.** Ask Moiz to run (or run with him):

```bash
brew install tursodatabase/tap/turso
turso auth signup            # opens browser
turso db create likhai-corpus
turso db show likhai-corpus --url          # → TURSO_DATABASE_URL
turso db tokens create likhai-corpus       # → TURSO_AUTH_TOKEN
```

Put both values in `.env`. **Blocked without this — ask, don't fake it.**

- [ ] **Step 2: DB client + migration**

`src/lib/server/db.ts`:

```ts
// Server-only Turso client. CONCEPT: Turso — SQLite (single-file database)
// hosted at the edge; @libsql/client speaks to it over HTTP.
import { createClient, type Client } from '@libsql/client';
import { env } from '$env/dynamic/private';

export function getDb(): Client | null {
  if (!env.TURSO_DATABASE_URL || !env.TURSO_AUTH_TOKEN) return null; // fail closed, caller decides
  return createClient({ url: env.TURSO_DATABASE_URL, authToken: env.TURSO_AUTH_TOKEN });
}
```

`scripts/migrate.ts`:

```ts
// One-shot schema setup. Run with: npm run migrate
import 'dotenv/config';
import { createClient } from '@libsql/client';

const url = process.env.TURSO_DATABASE_URL;
const authToken = process.env.TURSO_AUTH_TOKEN;
if (!url || !authToken) throw new Error('Set TURSO_DATABASE_URL and TURSO_AUTH_TOKEN in .env');

const db = createClient({ url, authToken });
await db.execute(`
  CREATE TABLE IF NOT EXISTS corpus_pairs (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    roman_text TEXT NOT NULL,
    urdu_text TEXT NOT NULL,
    engine TEXT NOT NULL,
    created_at TEXT NOT NULL DEFAULT (datetime('now'))
  )
`);
console.log('corpus_pairs ready');
```

Run: `npm run migrate` — Expected: `corpus_pairs ready`.

- [ ] **Step 3: Corpus route**

`src/routes/api/corpus/+server.ts`:

```ts
// Stores one consented Roman↔Urdu pair. No user data, no IP stored —
// the rate limiter sees the IP transiently but never writes it.
import { json } from '@sveltejs/kit';
import type { RequestHandler } from './$types';
import { getDb } from '$lib/server/db';
import { checkRateLimit } from '$lib/server/rate-limit';

const MAX_CHARS = 500;

export const POST: RequestHandler = async ({ request, getClientAddress }) => {
  if (!checkRateLimit(getClientAddress())) {
    return json({ ok: false }, { status: 429 });
  }
  const db = getDb();
  if (!db) return json({ ok: false }, { status: 503 });

  const body = await request.json().catch(() => null);
  const roman = typeof body?.roman === 'string' ? body.roman.trim() : '';
  const urdu = typeof body?.urdu === 'string' ? body.urdu.trim() : '';
  const engine = typeof body?.engine === 'string' ? body.engine : '';
  if (!roman || !urdu || roman.length > MAX_CHARS || urdu.length > MAX_CHARS || !['claude', 'dictionary'].includes(engine)) {
    return json({ ok: false }, { status: 400 });
  }

  await db.execute({
    // CONCEPT: parameterized query — values go in as data (?), never pasted
    // into the SQL string, which is what blocks SQL injection.
    sql: 'INSERT INTO corpus_pairs (roman_text, urdu_text, engine) VALUES (?, ?, ?)',
    args: [roman, urdu, engine],
  });
  return json({ ok: true }, { status: 201 });
};
```

- [ ] **Step 4: Wire consent in the page.** In `src/routes/+page.svelte`'s `convert()`, after `status = 'done';` add:

```ts
if (consent) {
  // Fire-and-forget: corpus trouble must never break the user's conversion.
  fetch('/api/corpus', {
    method: 'POST',
    headers: { 'content-type': 'application/json' },
    body: JSON.stringify({ roman: text, urdu: data.urdu, engine: 'claude' }),
  }).catch(() => {});
}
```

- [ ] **Step 5: Verify.** Dev server up: convert with consent OFF → `turso db shell likhai-corpus "select count(*) from corpus_pairs"` unchanged. Convert with consent ON → count +1 and the row reads correctly (`select * from corpus_pairs order by id desc limit 1`). `curl` a bad body → 400.

- [ ] **Step 6: Commit**

```bash
git add src/lib/server/db.ts scripts/migrate.ts src/routes/api/corpus src/routes/+page.svelte
git commit -m "feat: consented corpus pipeline into Turso"
```

---

### Task 10: Eval scoring library (TDD)

**Files:**
- Create: `src/lib/eval/score.ts`
- Test: `src/lib/eval/score.test.ts`

**Interfaces:**
- Consumes: nothing.
- Produces (used by Task 11):

```ts
export function editDistance<T>(a: readonly T[], b: readonly T[]): number;
export function characterErrorRate(reference: string, hypothesis: string): number; // 0 = perfect, can exceed 1
export function wordAccuracy(reference: string, hypothesis: string): number;       // 1 = perfect, floor 0
```

- [ ] **Step 1: Write the failing test**

`src/lib/eval/score.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { editDistance, characterErrorRate, wordAccuracy } from './score';

describe('editDistance', () => {
  it('is 0 for identical sequences', () => {
    expect(editDistance([...'کیا'], [...'کیا'])).toBe(0);
  });
  it('counts substitutions, insertions, deletions', () => {
    expect(editDistance([...'kitten'], [...'sitting'])).toBe(3); // classic
  });
  it('handles empty sequences', () => {
    expect(editDistance([], [...'abc'])).toBe(3);
  });
});

describe('characterErrorRate', () => {
  it('is 0 for a perfect conversion', () => {
    expect(characterErrorRate('کیا حال ہے', 'کیا حال ہے')).toBe(0);
  });
  it('is 1 when reference is empty but hypothesis is not', () => {
    expect(characterErrorRate('', 'x')).toBe(1);
  });
  it('scales by reference length', () => {
    expect(characterErrorRate('abcd', 'abce')).toBeCloseTo(0.25);
  });
});

describe('wordAccuracy', () => {
  it('is 1 for identical sentences', () => {
    expect(wordAccuracy('کیا حال ہے', 'کیا حال ہے')).toBe(1);
  });
  it('drops proportionally for one wrong word of three', () => {
    expect(wordAccuracy('کیا حال ہے', 'کیا حال تھا')).toBeCloseTo(2 / 3);
  });
  it('never goes below 0', () => {
    expect(wordAccuracy('ہے', 'یہ بالکل مختلف لمبا جملہ ہے')).toBe(0);
  });
});
```

- [ ] **Step 2: Run to verify FAIL** — `npx vitest run src/lib/eval/score.test.ts` — module not found.

- [ ] **Step 3: Implement**

`src/lib/eval/score.ts`:

```ts
// Scoring for the benchmark. Two views of "how wrong":
// CER (character error rate) is forgiving of small slips; word accuracy is
// the blunt "how many words came out right".
// CONCEPT: edit distance (Levenshtein) — the minimum number of single-item
// insertions/deletions/substitutions to turn one sequence into another,
// computed with dynamic programming (a table of best-so-far subproblem answers).
export function editDistance<T>(a: readonly T[], b: readonly T[]): number {
  const rows = a.length + 1;
  const cols = b.length + 1;
  const d: number[][] = Array.from({ length: rows }, (_, i) =>
    Array.from({ length: cols }, (_, j) => (i === 0 ? j : j === 0 ? i : 0)),
  );
  for (let i = 1; i < rows; i++) {
    for (let j = 1; j < cols; j++) {
      const cost = a[i - 1] === b[j - 1] ? 0 : 1;
      d[i][j] = Math.min(d[i - 1][j] + 1, d[i][j - 1] + 1, d[i - 1][j - 1] + cost);
    }
  }
  return d[rows - 1][cols - 1];
}

export function characterErrorRate(reference: string, hypothesis: string): number {
  if (reference.length === 0) return hypothesis.length === 0 ? 0 : 1;
  return editDistance([...reference], [...hypothesis]) / reference.length;
}

export function wordAccuracy(reference: string, hypothesis: string): number {
  const ref = reference.split(/\s+/).filter(Boolean);
  const hyp = hypothesis.split(/\s+/).filter(Boolean);
  if (ref.length === 0) return hyp.length === 0 ? 1 : 0;
  return Math.max(0, 1 - editDistance(ref, hyp) / ref.length);
}
```

- [ ] **Step 4: Run to verify PASS** — `npx vitest run src/lib/eval/score.test.ts` — 9 passed.

- [ ] **Step 5: Commit**

```bash
git add src/lib/eval && git commit -m "feat: eval scoring (edit distance, CER, word accuracy)"
```

---

### Task 11: Eval test set + runner

**Files:**
- Create: `eval/testset.json` (60 items)
- Create: `scripts/run-eval.ts`
- Create: `src/lib/data/benchmark-results.json` (generated, committed)

**Interfaces:**
- Consumes: both engines (Tasks 2–3), scoring (Task 10).
- Produces (used by Task 12): committed `benchmark-results.json`:

```ts
type BenchmarkResults = {
  ranAt: string; // ISO date
  engines: Array<{
    name: string;                  // 'dictionary' | 'claude'
    overall: { cer: number; wordAccuracy: number; failures: number };
    byTier: Record<string, { cer: number; wordAccuracy: number; count: number }>;
  }>;
};
```

- [ ] **Step 1: Build the test set.** `eval/testset.json`: 60 entries — tiers `clean` (20), `chaotic` (15), `slang` (10), `code_switching` (10), `poetry` (5). Format (first entries shown; generate the rest yourself following these exactly — natural sentences, varied length, chaotic tier MUST use mangled spellings):

```json
[
  { "id": 1,  "tier": "clean",          "roman": "kya haal hai",                       "reference": "کیا حال ہے" },
  { "id": 2,  "tier": "clean",          "roman": "main ghar ja raha hoon",             "reference": "میں گھر جا رہا ہوں" },
  { "id": 3,  "tier": "clean",          "roman": "aj mausam bohat acha hai",           "reference": "آج موسم بہت اچھا ہے" },
  { "id": 21, "tier": "chaotic",        "roman": "mje ni pta yr kdr h wo",             "reference": "مجھے نہیں پتہ یار کدھر ہے وہ" },
  { "id": 22, "tier": "chaotic",        "roman": "tm kha thy itni dair se",            "reference": "تم کہاں تھے اتنی دیر سے" },
  { "id": 36, "tier": "slang",          "roman": "scene on hai bhai full",             "reference": "سین آن ہے بھائی فل" },
  { "id": 46, "tier": "code_switching", "roman": "yaar kal meeting hai office mein",   "reference": "یار کل میٹنگ ہے آفس میں" },
  { "id": 56, "tier": "poetry",         "roman": "dil se jo baat nikalti hai asar rakhti hai", "reference": "دل سے جو بات نکلتی ہے اثر رکھتی ہے" }
]
```

- [ ] **Step 2: Human step — reference verification.** Moiz reads Urdu: he reviews all 60 references before results mean anything. Present the file, apply his corrections. **The benchmark is only as honest as this step.**

- [ ] **Step 3: Write the runner**

`scripts/run-eval.ts`:

```ts
// Runs every engine over the eval set, aggregates CER + word accuracy
// overall and per tier, writes committed JSON for the /benchmark page.
// Run with: npm run eval   (needs ANTHROPIC_API_KEY in .env; ~60 Claude calls ≈ pennies)
import 'dotenv/config';
import { readFileSync, writeFileSync, mkdirSync } from 'node:fs';
import { dictionaryEngine } from '../src/lib/engines/dictionary';
import { claudeEngineFromKey } from '../src/lib/engines/claude';
import { characterErrorRate, wordAccuracy } from '../src/lib/eval/score';
import type { ConversionEngine } from '../src/lib/engines/types';

type Item = { id: number; tier: string; roman: string; reference: string };
const items: Item[] = JSON.parse(readFileSync('eval/testset.json', 'utf8'));

const apiKey = process.env.ANTHROPIC_API_KEY;
if (!apiKey) throw new Error('Set ANTHROPIC_API_KEY in .env');
const engines: ConversionEngine[] = [dictionaryEngine, claudeEngineFromKey(apiKey)];

const results = [];
for (const engine of engines) {
  const scores: Array<{ tier: string; cer: number; wa: number; failed: boolean }> = [];
  for (const item of items) {
    const r = await engine.convert(item.roman);
    if (r.ok) {
      scores.push({ tier: item.tier, cer: characterErrorRate(item.reference, r.urdu), wa: wordAccuracy(item.reference, r.urdu), failed: false });
    } else {
      // A refusal on a real sentence is a full miss.
      scores.push({ tier: item.tier, cer: 1, wa: 0, failed: true });
    }
    console.log(`${engine.name} #${item.id} ${r.ok ? 'ok' : 'FAIL'}`);
  }
  const avg = (xs: number[]) => xs.reduce((a, b) => a + b, 0) / xs.length;
  const tiers = [...new Set(items.map((i) => i.tier))];
  results.push({
    name: engine.name,
    overall: { cer: avg(scores.map((s) => s.cer)), wordAccuracy: avg(scores.map((s) => s.wa)), failures: scores.filter((s) => s.failed).length },
    byTier: Object.fromEntries(
      tiers.map((t) => {
        const ts = scores.filter((s) => s.tier === t);
        return [t, { cer: avg(ts.map((s) => s.cer)), wordAccuracy: avg(ts.map((s) => s.wa)), count: ts.length }];
      }),
    ),
  });
}

mkdirSync('src/lib/data', { recursive: true });
writeFileSync('src/lib/data/benchmark-results.json', JSON.stringify({ ranAt: new Date().toISOString(), engines: results }, null, 2));
console.log('wrote src/lib/data/benchmark-results.json');
```

- [ ] **Step 4: Run it** — `npm run eval`. Expected: 120 progress lines, JSON written, dictionary word accuracy well below Claude's. Sanity-read a few Claude outputs vs references; if Claude scores suspiciously low, check the references (or the prompt), not just the engine.

- [ ] **Step 5: Commit**

```bash
git add eval scripts/run-eval.ts src/lib/data/benchmark-results.json
git commit -m "feat: eval test set, runner, first benchmark results"
```

---

### Task 12: `/benchmark` page

**Files:**
- Create: `src/routes/benchmark/+page.svelte`
- Modify: `src/routes/+page.svelte` (footer link to /benchmark)

**Interfaces:**
- Consumes: `benchmark-results.json` (Task 11), design system (Task 5).
- Produces: public page with per-engine overall scores + per-tier bars (custom SVG, no chart library — YAGNI).

- [ ] **Step 1: Build the page**

`src/routes/benchmark/+page.svelte`:

```svelte
<script lang="ts">
  // Public benchmark: dictionary baseline vs Claude, from committed eval
  // results. Static data at build time — no runtime cost, fully reproducible.
  import results from '$lib/data/benchmark-results.json';
  import PhoolPattiFrame from '$lib/components/PhoolPattiFrame.svelte';
  import ChamakDivider from '$lib/components/ChamakDivider.svelte';

  const TIER_LABELS: Record<string, string> = {
    clean: 'Clean', chaotic: 'Chaotic spelling', slang: 'Slang',
    code_switching: 'Code-switching', poetry: 'Poetry',
  };
  const ENGINE_COLORS: Record<string, string> = {
    dictionary: 'var(--electric-blue)', claude: 'var(--canary)',
  };
  const pct = (x: number) => Math.round(x * 100);
</script>

<main>
  <h1 class="latin">The Benchmark</h1>
  <p class="latin sub">
    60 Roman Urdu sentences, hand-verified references. Two engines: a 500-word
    dictionary baseline and Claude (claude-sonnet-5). Higher word accuracy is better.
  </p>

  <ChamakDivider />

  {#each results.engines as engine}
    <PhoolPattiFrame accent={ENGINE_COLORS[engine.name]}>
      <h2 class="latin">{engine.name === 'claude' ? 'Claude' : 'Dictionary baseline'}</h2>
      <p class="latin big">{pct(engine.overall.wordAccuracy)}<span class="unit">% word accuracy</span></p>
      <p class="latin small">CER {engine.overall.cer.toFixed(3)} · {engine.overall.failures} refusals</p>

      {#each Object.entries(engine.byTier) as [tier, s]}
        <div class="row">
          <span class="latin tier">{TIER_LABELS[tier] ?? tier} ({s.count})</span>
          <svg width="100%" height="18" aria-label="{TIER_LABELS[tier]}: {pct(s.wordAccuracy)} percent">
            <rect x="0" y="3" width="100%" height="12" fill="var(--lacquer)" stroke="var(--bone)" stroke-width="1" />
            <rect x="0" y="3" width="{pct(s.wordAccuracy)}%" height="12" fill={ENGINE_COLORS[engine.name]} />
          </svg>
          <span class="latin val">{pct(s.wordAccuracy)}%</span>
        </div>
      {/each}
    </PhoolPattiFrame>
  {/each}

  <p class="latin small">Last run: {new Date(results.ranAt).toLocaleDateString()} · <a href="/">← Likhai</a></p>
</main>

<style>
  main { max-width: 640px; margin: 0 auto; padding: 3rem 1.25rem; display: grid; gap: 1.5rem; }
  h1 { text-align: center; color: var(--canary); margin: 0; }
  .sub { text-align: center; opacity: 0.85; }
  .big { font-size: 2.6rem; font-weight: 700; margin: 0.25rem 0; }
  .unit { font-size: 1rem; font-weight: 400; opacity: 0.8; }
  .small { font-size: 0.85rem; opacity: 0.75; }
  .row { display: grid; grid-template-columns: 10rem 1fr 3rem; gap: 0.6rem; align-items: center; margin: 0.4rem 0; }
  .tier { font-size: 0.85rem; }
  .val { text-align: right; font-size: 0.85rem; }
  a { color: var(--electric-blue); }
</style>
```

- [ ] **Step 2: Link it.** In `src/routes/+page.svelte` add a footer inside `<main>`:

```svelte
<footer class="latin" style="text-align:center; opacity:0.8; font-size:0.9rem;">
  <a href="/benchmark" style="color: var(--electric-blue);">How good is it? See the benchmark →</a>
</footer>
```

- [ ] **Step 3: Verify in browser** — bars match the JSON numbers, page works on a phone-width viewport.

- [ ] **Step 4: Commit**

```bash
git add src/routes/benchmark src/routes/+page.svelte && git commit -m "feat: public benchmark page"
```

---

### Task 13: E2E tests + pre-flight checklist

**Files:**
- Create: `tests/e2e/test_converter.py`

**Interfaces:**
- Consumes: the full app; Moiz's Playwright harness at `~/.claude/tools/webapp-testing-scripts/with_server.py`.
- Produces: repeatable e2e suite; a completed pre-flight checklist.

- [ ] **Step 1: Write the e2e script** (mocks `/api/convert` — e2e must not spend money or flake on network):

`tests/e2e/test_converter.py`:

```python
# End-to-end test: drives a real browser against the dev server.
# /api/convert is mocked at the network layer so tests are free and deterministic.
import sys
from playwright.sync_api import sync_playwright, expect

def run():
    with sync_playwright() as p:
        browser = p.chromium.launch()
        page = browser.new_page()

        page.route(
            "**/api/convert",
            lambda route: route.fulfill(
                status=200,
                content_type="application/json",
                body='{"ok": true, "urdu": "کیا حال ہے"}',
            ),
        )

        page.goto("http://localhost:5173")
        expect(page.get_by_text("لکھائی")).to_be_visible()

        page.get_by_label("Likho — type Roman Urdu").fill("kya haal hai")
        expect(page.get_by_text("کیا حال ہے")).to_be_visible(timeout=3000)  # after debounce

        page.get_by_role("button", name="ٹرک پہ لگا دو").click()
        expect(page.get_by_role("dialog")).to_be_visible()
        expect(page.get_by_role("button", name="Download PNG")).to_be_visible()
        page.get_by_role("button", name="Close").click()

        page.goto("http://localhost:5173/benchmark")
        expect(page.get_by_text("The Benchmark")).to_be_visible()

        browser.close()
        print("ALL E2E CHECKS PASSED")

if __name__ == "__main__":
    run()
```

- [ ] **Step 2: Run it via the harness**

```bash
python3 ~/.claude/tools/webapp-testing-scripts/with_server.py \
  --server "npm run dev -- --port 5173" --port 5173 \
  -- python3 tests/e2e/test_converter.py
```

Expected: `ALL E2E CHECKS PASSED`. Fix the app (or selectors) until true — use the superpowers:webapp-testing skill for debugging.

- [ ] **Step 3: Run Moiz's pre-flight checklist** (from his CLAUDE.md) and fix findings:
1. **Reload** mid-conversion → page returns to idle cleanly, no stuck state.
2. **Failure** → offline devtools: convert shows retry; corpus save fails silently (by design — documented in code).
3. **Bad input** → server rejects empty/>500 chars/bad JSON on BOTH routes (already coded; verify with curl).
4. **Secrets** → no key in client bundle: `grep -r "sk-ant" .svelte-kit build 2>/dev/null` must be empty after `npm run build`; Turso token is a write-capable token on a corpus-only DB — acceptable, note it.
5. **Cost** → rate limits verified with a curl loop (11th request in a minute → 429).
6. **One source of truth** → themes only in `themes.ts`; engine names only from `engine.name`; reasons typed in `types.ts`. Confirm no drift.

- [ ] **Step 4: Run the full unit suite** — `npm test` — all green.

- [ ] **Step 5: Commit**

```bash
git add tests && git commit -m "test: e2e suite + pre-flight checklist fixes"
```

---

### Task 14: Deploy (GitHub + Vercel + prod env)

**Files:**
- Modify: `README.md` (what/why/stack/benchmark numbers/screenshots)

**Interfaces:**
- Consumes: everything.
- Produces: public GitHub repo under `moizbuilds`, live Vercel URL with env vars set.

- [ ] **Step 1: README** — short: what Likhai is, the truck-art story, benchmark headline numbers, stack, `npm run dev` + `.env.example` setup, corpus consent statement.

- [ ] **Step 2: Create the GitHub repo and push**

```bash
gh repo create moizbuilds/likhai --public --source . --push
```

- [ ] **Step 3: Deploy to Vercel.** Use the Vercel MCP `deploy_to_vercel` tool, or `npx vercel` if Moiz prefers CLI. Then set env vars in the Vercel project (Production): `ANTHROPIC_API_KEY`, `TURSO_DATABASE_URL`, `TURSO_AUTH_TOKEN`. Redeploy so they take effect.

- [ ] **Step 4: Verify production** — on the live URL: one real conversion, card download, `/benchmark` renders, consented save lands in Turso (`turso db shell likhai-corpus "select count(*) from corpus_pairs"`).

- [ ] **Step 5: Wrap up** — commit any final tweaks; update memory (`project-likhai.md`: shipped + URL; bump 30-in-30 count); give Moiz the 3-bullet "what to learn from this app" summary (strategy pattern + DI, debounce + fail-closed API design, eval-driven honesty); offer the LinkedIn post skill.

---

## Self-review notes (done at planning time)

- **Spec coverage:** converter flow (T4/T6), truck-art identity (T5/T7), card studio (T7/T8), corpus + consent (T9), two-engine benchmark (T2/T3/T10/T11/T12), error handling & guardrails (T4/T6/T9/T13), deploy (T14). Out-of-scope list respected — no accounts, no reverse direction, no runtime image gen.
- **Type consistency:** `ConversionResult`/`ConversionEngine` defined once (T2), consumed by T3/T4/T11. `CardTheme`/`CARD_THEMES` defined T7, consumed T8. `checkRateLimit(ip, now?)` defined T4, reused T9. Benchmark JSON shape defined T11, consumed T12.
- **Known judgment calls:** in-memory rate limit is per-serverless-instance (documented in code); corpus save is fire-and-forget by design; e2e mocks the paid API.
